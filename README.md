# Talkative — Skype-like demo

Ez egy demonstrációs projekt, amely bemutatja, hogyan lehet **audio-first** hívásokat, chat-et és kontaktlistát megvalósítani Node.js (WebSocket) és WebRTC (PeerConnection) segítségével.

### 🚀 Futtatás (Terminálban)

1.  **Lépj be a mappába** (ahova kicsomagoltad a ZIP tartalmát):
    ```bash
    cd talkative_skype_style
    ```

2.  **Telepítsd a függőségeket:**
    ```bash
    npm install
    ```

3.  **Indítsd el a szervert:**
    ```bash
    npm start
    ```
    Ekkor ezt fogod látni: `Server listening on http://localhost:3000`

4.  **Nyisd meg a böngésződben:**
    ```
    http://localhost:3000
    ```
    (Nyisd meg többször ezt a címet a teszteléshez!)